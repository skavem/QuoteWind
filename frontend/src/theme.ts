import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: '#25273b',
    },
    secondary: {
      main: '#009688',
    },
  },
})

export default theme